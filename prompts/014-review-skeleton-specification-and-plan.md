Date: 2026-09-14

Purpose: Review the approved skeleton specification and actionable plan before implementation.

Review docs/superpowers/specs/2026-09-14-application-skeleton-design.md and docs/superpowers/plans/2026-09-14-application-skeleton.md in /home/wild/code/personal/sonrisa-task against the approved scope in prompts/013-approve-skeleton-design-and-execution.md, AGENTS.md, and ADR-006. This is a bounded read-only pre-implementation review. Do not edit, stage, commit, contact external services, inspect credentials or IDE-local files, or delegate. The previous topology review is complete; do not repeat it except where the new spec or plan conflicts with it.

Check that the plan is actionable and minimal: root Sonrisa.sln, one src/Sonrisa.Web Razor Pages project, empty ProductDbContext, two justified package references plus local EF tool, external ConnectionStrings:ProductDatabase configuration, and separate liveness/readiness. Missing or malformed connection configuration must never prevent startup/liveness; readiness must use actual CanConnectAsync and return 503 on failure with no secret-bearing exception/log output. Check scoped context resolution, cancellation/timeout handling, design-time discovery without a connection, SDK/package compatibility assumptions, and the absence of product entities, migrations, schema writes, duplicate services, n8n coupling, fake product UI, and unnecessary abstractions/tests. Assess real-process smoke validation, separate Rider validation, safe credential handoff only after independent checks, and accurate documentation/evidence. The user explicitly authorized continuing after spec/plan review without another architecture approval. The feature branch uses the existing checkout to preserve the approved pending documents and Rider path; no unrelated .idea files are included.

Return APPROVE, APPROVE_WITH_MINOR_NOTES, or REQUEST_CHANGES, with concrete Critical/Important/Minor findings and the smallest corrections. Do not demand runtime checks before implementation exists. The controller is preparing the execution ledger and baseline checks independently while you review.

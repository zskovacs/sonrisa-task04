Date: 2026-09-14

Purpose: Implement and verify the approved Razor Pages skeleton, empty EF context, and separate health endpoints.

Implement Task 1 of the reviewed skeleton plan in /home/wild/code/personal/sonrisa-task on branch feat/application-skeleton. Read this first: .superpowers/sdd/2026-09-14-application-skeleton/task-1-brief.md is your requirements, with the exact values to use verbatim. Also read the linked specification, the plan's Global Constraints, AGENTS.md, and ADR-006. The specification and plan received APPROVE_WITH_MINOR_NOTES with no blocking findings; the sole review wording clarification affects controller acceptance, not runtime behavior.

You own only the root solution, SDK/tool manifest, src/Sonrisa.Web application files, and removal of src/.gitkeep described in the brief. You are not alone in the codebase: the controller owns documentation, prompt history, evidence, and IDE validation. Preserve all other edits and local IDE files, and accommodate concurrent documentation updates. Do not stage or commit. Do not spawn subagents. Do not contact shared DEV systems or inspect credentials. Use temporary runtime-only negative test inputs outside Git; never place connection-string values into tracked scripts, logs, evidence, or your report. The controller will handle real PostgreSQL credentials after all independent checks are complete.

The project currently has no application. SDK 10.0.401 and runtime 10.0.12 are installed; public NuGet metadata confirmed the specified releases. Use the current checkout, not another worktree, to preserve the approved pending docs and Rider path. Follow the brief's deferred context configuration and safe health-failure contract; malformed configuration must be caught within the health operation, and no exception object or connection value may enter the health result or logs. Health timeout and cancellation should bound real connection attempts. Do not add a testing project, migrations, product entities, frontend assets, n8n settings, or extra packages. Verify the two-reference package plan actually resolves a compatible EF family; report a concrete conflict before changing dependencies.

Write your detailed report to .superpowers/sdd/2026-09-14-application-skeleton/task-1-report.md. Include changed files, commands actually run, actual build/tool/context/HTTP results, negative-path and timeout observations, secret-sentinel checks, and any concerns. Store only sanitized outcomes. Return a concise status, one-line validation summary, and concerns. The controller will prepare independent checks and documentation while you implement, then dispatch task review.

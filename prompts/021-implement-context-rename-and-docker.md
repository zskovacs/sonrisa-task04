Date: 2026-09-14

Purpose: Implement the reviewed AppDbContext rename and application-only Docker testing support.

Implement Task 3 from .superpowers/sdd/2026-09-14-application-skeleton/task-3-brief.md in /home/wild/code/personal/sonrisa-task. Read that brief first and the Application-only Docker refinement in docs/superpowers/specs/2026-09-14-application-skeleton-design.md. Follow AGENTS.md. The plan corrections have been independently approved: all negative Docker tests must isolate both default dotenv loading and service env_file from any user .env; positive container readiness remains pending safe runtime configuration. You are not alone in this checkout: the controller owns documentation, prompts, evidence, and integration. Do not revert others' edits. Your ownership is limited to the context rename/file and its references in Program.cs and Health/PostgresHealthCheck.cs; root Dockerfile, compose.yaml, .dockerignore, .env.example; and creating an ignored empty .env only if absent. Keep ConnectionStrings:ProductDatabase unchanged. Preserve any existing .env without reading or overwriting it. Do not change other app behavior, dependencies, schema, solution structure, or historical files. Use Rider semantic rename where available; Rider now recognizes Sonrisa.Web and has built it successfully. Align the context filename. Docker Engine 29.8.0 and Compose v5.5.1 are available. Use the reviewed pinned SDK/runtime images and a non-root runtime, one web service, loopback host port 5180 to container 8080, optional dotenv runtime configuration, and no local PostgreSQL/n8n, volumes, product functionality, schema operations, or extra health tools. Validate build/context discovery plus actual Docker build/start/HTTP behavior/restart/recreate, service count, non-root execution, host binding, build-context exclusions, and synthetic dotenv preservation. Use disposable isolated empty/synthetic env inputs for all negative checks, never real shared DEV credentials. Keep output sanitized, never print resolved secret-bearing Compose configuration or container environments, and clean up only your task-owned containers/networks. No shared infrastructure operations. Do not add a test project or commit temporary smoke scripts; use scratch files outside tracked artifacts. Do not stage, commit, or dispatch subagents. Inspect your diff and write a full sanitized report with commands, outcomes, changed files, and limitations to .superpowers/sdd/2026-09-14-application-skeleton/task-3-report.md. Return the status, concise results, report path, and any blocker. The controller will arrange task and final reviews and the milestone commit after acceptance.

Date: 2026-09-14

Purpose: Review the workflow-owned recovery, transactional processing, and circuit-breaker design before finalizing the architecture.

Review the proposed n8n/PostgreSQL recovery design for milestone 2 in /home/wild/code/personal/sonrisa-task. This is a bounded, read-only architecture review, not implementation or final whole-branch review. Do not edit files, run workflows, access credentials, stage, commit, or delegate. Read AGENTS.md and ADR-005 for current ownership: application manages user conditions and Razor Pages; n8n directly accesses the product PostgreSQL database and owns validation, matching, deduplication, delivery retries, and circuit breaking. EF Core migrations own product schema. Separate n8n internal database remains untouched.

Critically review this candidate contract: three small scheduled responsibilities ingest canonical events into durable Pending records, evaluate Pending records, and deliver Pending notifications. A source/external-ID unique key identifies one immutable first-accepted event snapshot; repeated polling does not re-evaluate completed events. Evaluation reads current enabled rules, applies an allowlisted typed condition, inserts unique event/alert/channel intents with immutable content/destination snapshots, and marks the event Evaluated in one database transaction executed by one n8n Postgres operation. Never assume a transaction spans nodes. Unfinished Pending events are revisited independently of new ingestion. A new source failure must not stop evaluation or delivery.

Delivery workflows own persistent per-transport-profile Closed/Open/Half-open circuit state and delivery/attempt records in the product DB, accessed directly with dedicated credentials. No application HTTP claims, callbacks, circuit code, or retry service exists. The workflow atomically claims a due delivery with a stable attempt ID and bounded lease, at most one active attempt per profile; Half-open permits one probe. It commits before making one external send. Status reporting is idempotent. On a lost database response, retry the same operation identity, not an unrelated claim or send. A transport success followed by a crash/unknown response can cause duplicate external sends on recovery; user explicitly prefers automatic retry over losing notification. Expired leases recover as uncertain, are retried with capped backoff, and update circuit accounting once. Sent is monotonic; late success can stop future attempts but not retract an in-flight send; stale outcomes must not mutate a newer circuit/probe. Independent Slack and email profiles. Permanent destination errors fail only that delivery; dependency errors contribute to circuit breaking. Pending work stays durable while Open; cooldown allows a single probe. Workflow/runtime retries on the actual transport node must not bypass this gate. Tentative demo defaults are three consecutive dependency failures, 60-second cooldown, one probe, 30-second transport timeout, 120-second lease, and retry delay 5 seconds doubling to a 300-second cap; these are explicit engineering defaults, not user SLAs. Transient/uncertain retries do not expire after an arbitrary attempt count; permanently failed records remain visible.

Check crash windows, inconsistent matching snapshots, concurrent evaluation/claims/probes, query atomicity, callback/DB replay, unfairness or stuck half-open circuits, hidden application ownership, maintainability, and missing assumptions. Source-specific updates and feed freshness remain separate product semantics under discussion; do not infer a provider or change the confirmed first source. Return concrete Critical/Important/Minor findings and the smallest corrections. State that this is conceptual review, not runtime validation. The controller will draft the active documents and independently verify source/contract references while you review.

# Alert configuration implementation validation

Date: 2026-09-14. Milestone 4 on `feat/alert-configuration`, baseline `9de6b95`. This record is updated only after actual checks; pending work is not a passing result. The user approved the [specification](../../docs/superpowers/specs/2026-09-14-alert-configuration-design.md) in chat and authorized same-session execution under the [reviewed plan](../../docs/superpowers/plans/2026-09-14-alert-configuration.md).


**Revision notice:** The initial-design checks below are historical. The user subsequently replaced per-alert destination selection with shared per-user PostgreSQL settings and requested FluentValidation. Those earlier passing results do not establish revised-model acceptance. The amendment and its new results are recorded at the end.

## Preflight and repository isolation

The independent specification/plan reviewer returned **APPROVE**, with no Critical or Important findings. Scope includes two configuration tables, a single inline condition, configured-owner queries, fixed server-rendered forms, Tailwind compilation, and request logs/traces. It excludes database spans, authentication, n8n work, runtime tables and sends.

The controller created `feat/alert-configuration` in the existing Rider-connected checkout, preserving approved uncommitted documentation. The original prompt remains verbatim; subsequent approvals/clarifications are excluded from prompt history. Spec/plan are retained with the requested feature milestone commit, not extra milestones. Baseline CLI/Rider build results are recorded in [context evidence](2026-09-14-alert-configuration-context-review.md).

## Read-only migration-target verification

Before migration generation/application, PostgreSQL MCP returned `current_database() = sonrisa_dev`, PostgreSQL 18.6, and zero public tables. No row data or n8n internal persistence was inspected.

Independently, the controller resolved the existing Compose service environment using an explicitly empty CLI dotenv file. Expanded configuration stayed in process memory and was never printed or written. A temporary, ignored .NET/Npgsql metadata probe reused the existing provider assembly and received the connection only through its child-process environment. It checked the configured database name and actual `current_database()` against `sonrisa_dev`, returning only database identity, server version, public-table count and a target-match boolean. The actual connection was absent from emitted output. Result: target match true, PostgreSQL 18.6, zero public tables. No schema, data, role or transport setting was changed.

This verifies the application runtime target, not merely the MCP target. It does not by itself approve a future migration: source/SQL review and a fresh object-inventory check are still required before applying it. ADR-007's existing DEV privilege/TLS exception remains accepted; no new security claim is made.

## Work remaining at this checkpoint

Persistence/UI/telemetry implementation, task reviews, migration review/application, PostgreSQL behavior tests, browser checks, CSS/Docker reproduction, telemetry export/privacy checks, final review and the milestone commit remain pending. No feature implementation acceptance is claimed by this initial record.

## Persistence task and first PostgreSQL verification

Task review returned **APPROVE_WITH_MINOR_NOTES**, with no Critical/Important findings. Local test-first work reached 33 passing tests; three database tests were explicitly skipped until application. Build passed with zero warnings/errors. Review noted one redundant tracker-clear call for cleanup.

Meaningful corrections: destination regex end anchors admitted a trailing LF and validation normalized some newline input; added cases reproduced and corrected these. Malformed connection configuration initially escaped the safe result boundary; a bounded parse check now returns Unavailable. EF assemblies resolved at mixed patch levels, so explicit EF Core/Relational 10.0.12 references aligned the graph. The design-time factory now resolves User Secrets then environment settings, retains provider-only offline generation, and needs no hardcoded or command-line connection value.

The controller generated and inspected `dotnet ef migrations script --project src/Sonrisa.Web --no-build`. SQL creates only two configuration tables, expected keys/checks/indexes, and EF migration history. No destructive statement exists in the forward script. Immediately before application, MCP reported an empty public relation inventory and the independent application-connection probe again matched `sonrisa_dev`, PostgreSQL 18.6, zero public tables.

`dotnet ef database update --project src/Sonrisa.Web --no-build` completed successfully with the verified connection supplied only through a child-process environment. Migration `20260914125733_InitialAlertConfiguration`, EF 10.0.12, is present in history. Read-only PostgreSQL MCP inspection confirmed all column names/native types/nullability, two table primary keys, channel foreign key/cascade, textual-code checks, finite-number check, UUID/name/destination checks, owner index and enabled-event partial index. Only the two product tables plus EF history exist.

The controller then ran `dotnet test Sonrisa.sln --no-build --no-restore -v:q` with both guarded test variables and verified target name: **36 passed, 0 skipped, 0 failed**. This exercises actual PostgreSQL owner isolation, stale revisions, channel reconciliation, retired-allowlist disable, NaN rejection and caller-transaction rollback with a separate observer. It does not yet establish failed-child SaveChanges rollback or browser/telemetry behavior; those remain pending.

## Aggregate transaction boundary

The controller added a guarded PostgreSQL test using one committed, uniquely owned fixture. A SaveChanges interceptor supplies an invalid child destination after management validation; the real PostgreSQL CHECK rejects it. An independent context confirms the entire old alert/channel/revision snapshot survives the failed write. For a successful replacement, a transaction-commit interceptor reads through an independent connection immediately before commit and still sees the original snapshot; a read after commit sees the complete changed name/number/status/channel/revision together. The exact owned fixture is removed in finally, with both alert and owner predicates.

`dotnet test Sonrisa.sln --no-build --no-restore --filter FullyQualifiedName~PostgresAtomicityTests -v:q`, with guarded target configuration: **1 passed, 0 skipped, 0 failed**. This is a real PostgreSQL failure/visibility check, without a manually supplied outer write transaction, database reset, test server, or runtime-workflow implementation.

## Razor forms, styling and browser acceptance

Task 2 local checks passed: seven focused HTTP/form tests, `npm ci` (34 packages, zero reported vulnerabilities), `npm run css:build` (Tailwind 4.3.3), solution build (zero warnings/errors), Docker build, and runtime CSS/no-Node/no-SDK/no-source/no-.env inspection. Task review returned APPROVE_WITH_MINOR_NOTES; it correctly retained missing-token and real database/browser flows as controller acceptance checks.

The Chrome MCP connector could not find an active browser, and CUA reported no enabled surfaces. The controller used an existing local Playwright installation and cached Chromium 151.0.7922.34 in an isolated headless context; no project browser dependency was added. Browser checks targeted only the owned loopback validation host with generated owner and synthetic destinations. Initial script selectors were corrected for duplicate Create links and the alert-name edit link; these were harness assumptions, not product defects.

Actual browser acceptance exposed a product defect missed by unit/test-host checks: Razor rendered the hidden desired-state boolean as `value="value"`. The focused fix uses explicit string values `true`/`false`. A proposed conversion helper/test was removed because it did not test the failing rendering boundary. Scoped review approved the fix; actual Chromium Enable then Disable passed. Earlier HTTP binding checks also reproduced an invalid checkbox conversion, leading to safe ModelState guards for editor/status/revision inputs.

Chromium exercised create, rejected NaN with retained input, edited threshold and replaced email with Slack, enabled/disabled, and inspected the desktop/editor/mobile pages. At 390px viewport the document width was 390px, CSS loaded and no owner field existed. The controller visually inspected [desktop editor](alert-configuration-ui/alert-editor-desktop.png), [mobile editor](alert-configuration-ui/alert-editor-mobile.png), and [list](alert-configuration-ui/alert-list-desktop.png). These contain synthetic fixture data only. This is basic visual/usability verification, not a full accessibility audit.

Separate real HTTP requests verified missing antiforgery rejection (400), unlisted destination rejection, ignored forged OwnerId fields, and cross-owner list/get/edit/status isolation using two local hosts with distinct generated owners. Foreign get/edit/status returned 404. Stale edits preserved the newer values, and invalid status binding returned 400. Both fixture configurations survived a full host-process restart; liveness and readiness returned 200 afterward. No emails/Slack messages or n8n calls occurred.

## Permanent rendered-status regression

`PostgresPageTests.Rendered_status_forms_enable_and_disable_the_owned_alert` initially failed because its WebApplicationFactory late configuration hook did not reach Program's eagerly resolved owner/destination settings. The normal external-configured host already passed the same product flow. The fixture now replaces only external settings/current owner and typed PostgreSQL options in test DI, retaining the actual service/EF/Razor pipeline. This is a harness correction, not a production ownership redesign.

The guarded test then passed (**1 passed, 0 skipped, 0 failed**) using actual PostgreSQL. It creates one uniquely owned fixture through HTTP, rejects a missing antiforgery token, ignores a forged owner field, reads exact `true`/`false` values from rendered status forms, posts those forms, and verifies both resulting database states. Cleanup uses the exact fixture ID and generated owner. This closes the task review's missing-token coverage note and protects the browser-discovered rendering failure.

## OpenTelemetry task review and container acceptance

The telemetry task review returned **APPROVE** for specification and code quality, with no findings. Eleven focused tests captured SDK spans/correlated logs and actual HTTP/protobuf export to a temporary in-process loopback receiver. They verified common signal suffixes, exact signal override paths, headers/service identity, malformed-setting isolation, unavailable endpoint behavior and sensitive request-sentinel exclusion. A malformed header-name case first failed and then passed after token/control-character validation. Console JSON intentionally excludes scopes/identifiers; correlation evidence applies to SDK/OTLP logs. No gRPC receiver, production backend or database spans were validated.

The initial full PostgreSQL-enabled run returned **47 passed, 9 failed**: telemetry tests inherited the externally supplied runtime database connection and received healthy management responses instead of their intended no-database result. This revealed test-environment isolation, not an exporter availability dependency. The telemetry fixture is being corrected to isolate/restore external settings; the final rerun is recorded separately below.

The controller built the final production image using the actual root Compose definition plus an owned validation override. Negative testing explicitly cleared service env_file and used an empty CLI dotenv; it did not load the user's .env. The separate project published only 127.0.0.1:5191. With no connection or OTLP endpoint: liveness 200, readiness 503, alerts list 503, create form 200, CSS 200 (10,185 bytes); restart liveness passed. Positive recreation used the previously verified runtime connection only in child-process environment: liveness/readiness/CSS 200 and both owned persisted fixture values were readable. Positive restart and subsequent readiness/list checks passed.

A runtime image check confirmed non-root UID, nonempty CSS, and no Node executable, node_modules, SDK, source tree or .env. An additional real host process with healthy PostgreSQL and an unavailable loopback OTLP endpoint served management, liveness and readiness with HTTP 200. The ordinary no-endpoint host likewise remained healthy. No shared infrastructure, roles, n8n storage/workflows or transport sends were changed.

## Final combined checks and cleanup

After telemetry environment isolation was corrected and independently re-reviewed (**APPROVE**), `dotnet test Sonrisa.sln --no-build --no-restore` with the guarded PostgreSQL target passed **56 tests, 0 skipped, 0 failed**. This includes all five relational cases and all eleven telemetry cases. The preceding final CLI build succeeded with zero warnings/errors. Rider build session `4acf04dd-6d21-4550-853a-2ac206e54b9c` completed successfully with no problems. Focused Rider inspection found weak style suggestions and one unused logging import; no correctness error was reported.

A private in-memory check examined three actual current host/container logs (4,062 characters) against the resolved connection/password and generated owner/alert IDs, synthetic destination and query sentinels. No match was found; only counts/booleans were emitted. SDK capture/wire tests separately verified telemetry sentinels, so console-log checks are not substituted for exporter inspection.

The controller stopped only the owned validation hosts and Compose project, then removed exactly two browser/HTTP fixture IDs with the generated owner predicate. No truncate/reset or unrelated cleanup occurred. PostgreSQL inspection after cleanup confirmed zero alert/channel rows; the reviewed product schema and migration history remain. Runtime workflows, sends, authentication, production access/TLS, gRPC wire behavior, an external telemetry backend and a full accessibility audit remain unimplemented or unvalidated as specified.

## Shared user destinations amendment

The user rejected application-configuration destination allowlists and bespoke validation, then explicitly selected common destinations per user. ADR-010 and the amended specification/Tasks 5–6 record the resulting users/alerts contract and manual FluentValidation 12.1.1 integration. NuGet package metadata confirms Apache-2.0 licensing and .NET 8+ compatibility; the official FluentValidation ASP.NET documentation recommends manual invocation over the legacy automatic MVC pipeline. No Identity or authentication requirement was introduced.

The first amendment preflight requested changes for three real migration gaps: different per-alert channel subsets could be silently unioned; writes during backfill could lose a destination; a generated Down could discard profiles. The reviewed amendment requires identical complete channel sets per owner, transaction-scoped exclusive locks before validation/transfer, and an explicit unsupported downgrade before any operations. Scoped re-review returned APPROVE. This approves the plan only; revised code/database acceptance remains pending.

Before the revised migration, read-only PostgreSQL inspection again returned sonrisa_dev, PostgreSQL 18.6, only alerts/alert_channels/EF history, and zero alert/channel rows. The independent application-connection probe also matched sonrisa_dev and three public tables. No migration or data change occurred in this check.

The revised local suite passed 54 tests with 11 guarded PostgreSQL tests explicitly skipped; CLI build and Tailwind CSS build passed. The controller then ran the five migration-conversion tests with the verified DEV connection: **5 passed, 0 skipped, 0 failed**. They execute the migration’s guard/transfer statements against session-local temporary tables and cover identical targets, different values, different channel presence, absent channels, and invalid legacy Slack format. They do not apply the full migration or alter public tables.

The focused Task 5 review returned **APPROVE**, with no findings. Controller source/SQL review confirmed START TRANSACTION, old-table locks before validation/copy, expected users creation/owner FK, only the replaced alert_channels drop, migration-history insertion and COMMIT. All owned old hosts and the validation Compose project were stopped. A fresh target inventory still showed zero legacy rows and no users collision. Applying `20260914140740_SharedUserNotificationDestinations` succeeded. Actual PostgreSQL inspection confirmed users/alerts plus EF history, both migration records, UUID owner FK with RESTRICT, native textual condition codes/finite double, profile destination checks, and only the two known alert access indexes plus primary keys. No roles or unrelated objects changed.

The first revised full PostgreSQL-enabled test run returned **64 passed, 1 failed**. `AlertPagesTests.Settings_form_exposes_shared_destinations_and_antiforgery` inherited the runtime connection and returned 200 instead of its intended no-database 503. The actual settings page worked; this is another test-fixture isolation gap and is being corrected before acceptance.

Chromium 151.0.7922.34 verified settings-first guidance, profile creation/editing, invalid email/Slack rejection, alert creation/editing, NaN rejection, enable/disable, and absence of per-alert destination/owner fields. The invalid-email probe deliberately bypassed browser-native validation to exercise server validation. At 390px both editor and settings document widths remained 390px. Current screenshots: [notification settings](alert-configuration-ui/shared-settings-desktop.png), [mobile settings](alert-configuration-ui/shared-settings-mobile.png), [alert editor](alert-configuration-ui/shared-editor-desktop.png), [mobile editor](alert-configuration-ui/shared-editor-mobile.png), [list](alert-configuration-ui/shared-list-desktop.png). Earlier screenshots above remain historical.

Separate HTTP checks passed missing antiforgery rejection, forged profile/alert OwnerId ignored, foreign-profile read isolation, foreign alert list/get/edit/status isolation, stale profile/alert preservation, and invalid status binding rejection. A parameterized direct SQL join over the two exact owned fixtures confirmed both share the updated user destinations and retain gte/number codes. Full host restart preserved both alerts and the common settings; liveness/readiness remained 200.

Reproducibility: `npm ci` succeeded (33 installed packages, 34 audited, zero reported vulnerabilities; npm reported an optional @parcel/watcher install-script policy warning), Tailwind 4.3.3 regenerated CSS, CLI build passed with zero warnings/errors, and Rider build session `a07adaa2-925c-40ec-94d6-960524ebab12` completed successfully with no problems. The actual Compose-based image rebuilt successfully. Without database/OTLP configuration: live 200, ready 503, alerts 503, create 200, settings 503, CSS 200 (10,692 bytes). With the verified database: all relevant pages/health/CSS returned 200 and persisted shared settings were readable. Container restart retained state. Runtime checks passed non-root execution, CSS presence, and absence of Node, SDK, source, node_modules and .env. A real healthy-database host with unreachable loopback OTLP remained available on all management/health routes.

The HTTP fixture isolation fix was independently re-reviewed (**APPROVE**). The final guarded full suite passed **65 tests, 0 skipped, 0 failed**, including all eleven PostgreSQL cases and eleven observability cases. Profile atomicity verification uses an explicit test transaction for independent before/after-commit snapshots and a real constraint-rejected update for rollback; production profile updates remain one atomic SQL update without an added transaction abstraction. The first-create race test commits a competing profile between the original missing-row read and save, then verifies a safe conflict and preserved winner.

Focused Rider analysis of the settings service, validator and PageModel reported only weak style suggestions, no errors. The controller inspected all five revised screenshots. A private in-memory audit of five real host/container logs (6,285 characters) found no resolved connection/password, fixture owner/alert identifiers or synthetic destination/query sentinels. Exported-log/trace privacy and correlation were separately exercised in the passing telemetry tests.

Cleanup stopped only owned validation hosts/Compose and removed exactly two fixture alerts and their one generated profile. Read-only PostgreSQL verification confirmed zero users/alerts and two retained migration records. No default profile was fabricated. Downgrade remains deliberately unsupported; gRPC wire export, production observability backend, database spans, full accessibility audit, real authentication, n8n ingestion/evaluation and notifications were not validated or implemented.

## Final-review whitespace constraint correction

The whole-branch reviewer requested one Important correction: plain btrim accepted a tab-only alert name or sole email destination despite the specified nonblank DB contract. Read-only SQL confirmed both cases and showed that the current PostgreSQL POSIX whitespace class does not include NBSP. The forward migration `20260914143004_EnforceUnicodeTrimmedConfiguration` uses an explicit Unicode whitespace set matching .NET Trim and changes only the two CHECK constraints; both earlier applied migrations remain unchanged. Its Down restores earlier checks without dropping data.

Before application, the two new guarded direct-SQL tests failed as expected against the existing DEV constraints: **0 passed, 2 failed**, each because the invalid insert did not throw. Their test transactions rolled back all generated rows. Local build and 54 local tests passed; 13 relational tests were skipped locally. The architecture diagram’s inaccurate secure-connection label was also corrected. Source/SQL review, application and green results follow below.

Scoped independent re-review approved the forward migration for controlled application. Fresh runtime/MCP target verification returned sonrisa_dev, the expected three public tables, zero users/alerts, and two prior migration records. Applying `20260914143004_EnforceUnicodeTrimmedConfiguration` succeeded. PostgreSQL catalog inspection confirmed the explicit Unicode trim character sets in both checks and all three migration records. The final full suite then passed **67 tests, 0 skipped, 0 failed**, including both new direct-write regression tests (tab, newline and NBSP in each). No invalid rows survived the test transactions.

The final source image rebuilt, and its health, alerts, settings and CSS smoke checks all returned 200 against DEV. Rider build `35f3a7e3-3a7e-49b2-aef0-974277905f74` passed with no problems. `dotnet ef migrations has-pending-model-changes --project src/Sonrisa.Web --no-build` reported no differences. The owned Compose project was stopped; PostgreSQL verification again showed zero users/alerts and three retained migrations. The earlier browser/telemetry acceptance remains applicable: this last correction changes only database trim constraints/model metadata and corresponding tests, with no UI/export behavior change.

The final independent whole-branch verdict is **APPROVE**, with no open Critical, Important or Minor findings; [the full review](2026-09-14-alert-configuration-final-review.md) preserves the finding and evidence-closure history. The controller inspected the staged milestone file inventory and diff, checked whitespace errors and excluded paths, and compared candidate contents against the actual runtime connection/password in memory without exposing values; no match was detected. Documentation link checks reported no missing local targets.

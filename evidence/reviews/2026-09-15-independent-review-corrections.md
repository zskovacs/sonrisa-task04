# Independent review assessment and corrections

Date: 2026-09-15

## Baseline and method

The two reviews supplied in [prompt 030](../../prompts/030-assess-independent-reviews.md) assessed `e667447`. This pass started with clean `main` and `docs/finalize-submission` at `ce8b76c`, which already contains the completed retrospective, runbook and documentation reconciliation. The existing documentation branch was selected for the corrections. Review **A** is the first review; **B** is the second. Their numbered findings are mapped below; supplied review claims are not new verification evidence.

This assessment uses local source, existing evidence and Git history, focused reproductions and local tests. It does not repeat the reviewers' claimed full-history secret scan, inspect shared DEV, run n8n, or send notifications. The original evaluation DOCX is not present in the checkout or attached to this session: the quoted “all prompts” requirement is assessed conditionally, not certified against an unseen document.

Git also corrects one non-numbered claim in review B: the original `4fca2f1` plan had **ten** phases and planned milestone commits, not nine. The nine-milestone sequence is the later reviewed plan. Both versions remain accessible; the README now links the original directly.

## Submission and architectural findings

| Review IDs | Assessment and disposition | Evidence / rationale |
| --- | --- | --- |
| A H3; B H3 | **Partly valid process gap; clarify, preserve history.** A curated task archive is not all prompts or a full transcript. Restore neither excluded decision replies nor agent execution prompts against the explicit user policy. | [Prompt index](../../prompts/README.md) now links the pre-cleanup tree, explains drafting provenance limits and historical number reuse. `git diff-tree --diff-filter=D 9de6b95` lists **22**, not 23, deleted files. Git preserves those files but cannot reconstruct conversations never retained. If the quoted submission requirement is literal, that completeness gap remains. |
| A H4 | **Outdated at current HEAD.** | [Retrospective](../../docs/final-reflection.md) was completed in `ce8b76c`. This pass adds the concrete cost of replacing the runtime and the lesson to agree loss/recovery semantics before building persistence. No person-hour estimate is invented. |
| A H1 | **Real capacity limit; “47 days” is an estimate.** Keep capacity/loss prominent; defer operational redesign. | [Recorded cap experiment](2026-09-14-end-to-end-validation.md) and ADR-012 establish failure before filtering. One day's externally measured feed volume does not establish a fixed deadline; incoming overlap also counts in the cap check. The workflow is inactive. Minimum-threshold filtering postpones saturation and changes rematching semantics; date prefixes do not evict keys; clearing history permits repeats. Automatic clearing was explicitly rejected. |
| A H2; B H2 | **Real accepted best-effort loss.** Prominent README disclosure added; dedup reordering/recovery deferred. | [ADR-012](../../docs/adr/ADR-012-use-n8n-native-runtime-state.md) explicitly supersedes D-002. Moving dedup after a successful query could reduce database-failure exposure, but requires a revised envelope/key contract and native failure/replay validation. It cannot quantify “halving” risk or solve transport exhaustion. Marking seen after fan-out also needs rules for partial channel success; a new processed-events table restores rejected runtime persistence. |
| B H1 | **Accurate operational limit; activation is outside the accepted task.** | Manual/inactive operation is explicit in ADR-012 and the completed milestone requirements. Real-source normalization and real transport acceptance were demonstrated separately; unattended real-USGS-to-real-recipient delivery was not. No schedule, Error Trigger workflow, live run or operator notification was authorized by this correction pass. |
| A M4; B M2 | **Accurate extension cost, not a requirement for a generic schema now.** | ADR-010/012 deliberately select shared explicit destination fields. Email demonstrated a second routing branch, not zero-cost arbitrary channels. A third channel should trigger a concrete configuration decision; a table/JSON registry/subworkflow architecture is deferred. Independently specified exporter tests remain useful checks rather than deriving every expected value from the implementation. |
| B M3 | **Accurate single-event scope.** | [Configuration contract](../../docs/06-alert-configuration-contract.md) already requires an explicit extension for new fields/operators/types. Typed codes give a deliberate extension direction, not a plugin system or already-supported news/markets. |
| A M5 | **Accepted product decision; claim that it is merely an architectural consequence is incomplete.** | [Prompt 027](../../prompts/027-add-operational-admin-view.md), D-010 and the admin specification define read-only configuration administration. Runtime outcomes remain in n8n; exposing them through the app needs a new product/access/integration decision. |
| A M6 | **Accurate accepted limit.** | README explicitly states no authentication/authorization and that configured ownership is not a security boundary. Loopback/AllowedHosts are development configuration, not a replacement for access control. Preserve the existing warning before public/multi-user use. |
| A M3; B M7 | **Real sequential cost and per-event queries; proposed remedies require design.** | Native five-attempt timing is recorded in [integrated evidence](2026-09-14-end-to-end-validation.md). Roughly 20 seconds per exhausted item is retry wait alone, excluding transport latency. Overlap is a risk, not proof of duplicates in every outage. A second visual branch does not by itself establish parallel processing; a blanket per-workflow “prevent concurrency” setting was not established for this installed release. Query batching, execution limits and per-channel short-circuiting remain future scale/operation work. |

An Error Trigger responds to a **failed execution**. This workflow deliberately handles exhausted sends and continues, so adding an error workflow alone would not cover every discarded item; this is an inference from the current graph and the [official error-handling contract](https://docs.n8n.io/build/flow-logic/handle-errors-gracefully). It would also add a second workflow and an operator-notification path outside the accepted scope.

## Defects selected for correction

| Review IDs | Assessment and selected correction | Verification target |
| --- | --- | --- |
| A M1; B M4 | **Valid unexpected-exception logging gap.** Add a sanitized exception boundary and suppress hosting-lifetime telemetry; retain raw framework/provider privacy filters. A request status span already existed, so “invisible in every sink” overstates the entire observability picture. | Synthetic throwing endpoint: generic 500, exception classification and trace correlation, no raw exception/URL/query/credential sentinel in logs or OTLP. Existing safe 503 responses remain covered. |
| A M2; B M5 (Email) | **Valid cross-layer destination mismatch.** Align application/evaluator with the existing Email preparer's conservative contract using common test vectors. | Application and both runtime checks reject unsupported local-only domains, local dot errors, malformed domain labels and controls; normalized valid destinations remain accepted. Database constraints intentionally remain structural rather than a full email parser. |
| B M1; A M8 (portability) | **Valid test portability defects.** Scope the malformed-configuration assertion to the warning and use `fileURLToPath` for the Node child process. | Deliberate `-1` content-root collision; copied checkout path containing spaces. The latter initially produced 7 passed / 1 failed in the eight process tests with `MODULE_NOT_FOUND` for a percent-encoded path. Historical successful runs remain true for their recorded environment. |
| A L1; B L1 | **Valid display inconsistency.** Format the owner alert list threshold with invariant `R`, matching the editor and admin. | Exact invariant expression review and Razor compilation; no fresh populated-page/browser or database check. Do not globally enable invariant globalization. |
| A M7; B L2 | **Valid usability improvement.** Use a bounded, safely escaped alert name in Slack and keep identifiers in diagnostic data. | Normal/malicious/empty names, synthetic marker and no internal IDs in user-facing Slack text. The older no-ID message rule explicitly applied to Email; it was not already a universal Slack contract. |
| A L8; B L3 (Slack timestamp) | **Valid missing defensive check.** Require a parseable timestamp and render UTC. | Missing/invalid timestamp fails with a fixed diagnostic-safe error, rather than rendering `undefined`. |
| A M8 (CI); B M6 (unguarded reproducibility) | **Useful bounded recommendation.** Add CI for documented local CSS/build/.NET/Node checks without database credentials. | Run the same commands locally and validate workflow structure. Hosted Actions execution is separate and remains unverified until pushed. |

## Deferred polish and review corrections

| Review IDs | Assessment and disposition | Evidence / rationale |
| --- | --- | --- |
| A L2; B M5 (database helpers) | **Valid duplication; defer extraction.** | Three narrow helpers can drift, but the previously divergent behavior is already corrected and covered. Extracting them is a maintainability refactor, not necessary to repair the selected defects. |
| A L3 | **Valid UX inconsistency; defer.** | Some 409/503 paths return bare/plain responses while others render friendly pages. Status semantics are intentional and tested; redesigning all failure pages is separate polish. The new unexpected-error response covers exceptions, not every intentional status result. |
| A L4; B L4 | **Valid external-provider limitation; document.** | `sonrisa@example.test` is intentionally pinned for SMTP4DEV. A real sender requires an approved provider and coordinated SDK/exporter/artifact changes. It is not universally true that every SMTP server rejects `.test`, but it is unsuitable as an internet-delivery sender. No provider was selected here. |
| A L5; B L5 (commit titles) | **Accurate preserved history.** | Both runtime commits use the requested milestone title. [Milestone history](../../docs/01-plan.md) differentiates initial implementation and deliberate replacement; do not rewrite shared history. |
| A L6; B L5 (hygiene) | **Mostly optional cleanup; preserve.** | Empty `.gitkeep` directories are clearly labeled non-evidence, not false test results. The legacy SMTP fixture and restricted Code runner are not on the current workflow build path; Email result tests provide the necessary paired-item context directly. Migration history must preserve applied create/drop operations. No license is chosen on the user's behalf, and vendored skills/tooling remain intact. |
| A L7 | **Valid one-hour coverage limit; a longer feed is not a free guarantee.** | The hour feed and lack of cursor are explicit scope. A day feed broadens first-run matches and input size against the existing 1,000-record/2 MiB bounds, while leaving history capacity and failures unresolved. Revisit with an unattended-operation requirement. |
| A L9; B L5 (infrastructure IDs) | **Non-secret exposure; optional hygiene deferred.** | Current docs use the DEV workflow link for operator navigation, and historical records contain environment identifiers. No newly supplied real credential was identified in the changed review record. Do not rewrite retained evidence/prompts merely to hide non-secret historical context. |
| B L3 (paired items) | **Correct constrained coupling, not a reproduced defect.** | `record-email-result` relies on batch size one and paired origin; exporter checks enforce that exact contract. Preserve and test it; reconsider only when changing batching. |
| B L6 | **Documented setup prerequisite; CI added.** | README already requires `npm ci` and `npm run css:build`; Docker builds CSS. Automatically invoking npm from every .NET build adds tooling/network side effects. Keep the explicit local sequence, now exercised in CI. |
| B M6 (remaining coverage) | **Useful future coverage proposals; no fabricated proof.** | Handshake timeout has recorded manual evidence; guarded tests require externally supplied credentials. This pass adds relevant regressions but does not provision PostgreSQL/Testcontainers or rerun shared DEV writes. Raw historical TRX files and the untracked helper are not reconstructed or claimed reproducible here; the runbook documents the direct supported test entry point. |
| B M8; A process §3.4 | **Navigation already improved in ce8b76c; add direct history/tooling links.** | README/runbook/ADR index and retrospective already provide the short current view. Link the original plan at its immutable revision instead of duplicating it. Keep factual uncertainty statements where they distinguish mock, live and untested behavior; do not delete decision history or bundled skills for cosmetic word-count reduction. |

## Changed files

- Application: `Users/UserNotificationSettingsInputValidator.cs`, `Observability/SafeExceptionHandler.cs`, `Observability/ObservabilityExtensions.cs`, `Program.cs`, and `Pages/Alerts/Index.cshtml` under `src/Sonrisa.Web/`.
- Tests/runtime: `DestinationContractTests.cs`, `ObservabilityTests.cs`, test-project fixture copying, `tests/fixtures/destination-contract.json`, `n8n/runtime/evaluate-alerts.js`, `n8n/runtime/prepare-slack-message.js`, their tests, the portable process test, and the two corresponding Code bodies in `n8n/workflows/process.json`.
- Automation/process: `.github/workflows/ci.yml`, the current prompt/index, correction plan, this assessment, and linked README/runbook/contracts/history/reflection/review evidence documentation. No dependencies, migrations, database schema, transport credentials or workflow graph changes.

## Verification results

The following results describe the local correction working tree based on `ce8b76c`, not a new committed milestone or a hosted deployment. Local tools: .NET SDK 10.0.401, Node 24.21.0, npm 11.19.0.

| Command / check | Actual result |
| --- | --- |
| `rtk proxy npm ci` | Passed; 33 packages added, 34 audited, zero reported vulnerabilities. npm warned that the optional watcher install script was not approved; the subsequent CSS build succeeded. |
| `rtk proxy npm run css:build` | Passed with Tailwind 4.3.3. |
| `rtk proxy dotnet build Sonrisa.sln -c Release` | Passed, zero warnings/errors. The later one-line error-copy correction was rebuilt by its focused Release test. |
| `env -u SONRISA_TEST_DATABASE -u SONRISA_TEST_DATABASE_NAME rtk proxy dotnet test Sonrisa.sln -c Release --no-build --logger 'trx;LogFileName=independent-review-final.trx' --results-directory artifacts/review-test-results` | **69 passed, zero failed, 19 skipped; total 88.** Final TRX counters inspected locally; raw runner paths/output remain in ignored artifacts. |
| `rtk proxy node --test n8n/tests/*.test.mjs` | **30 passed, zero failed/skipped.** |
| Node suite in a copied path containing spaces and `-1` | **30 passed** after reproducing the original builder failure (7 passed / 1 failed) with the percent-encoded path. |
| Focused destination/validator .NET checks; observability suite | **27/27** and **14/14** passed respectively. New tests first reproduced destination mismatches, missing exception handling and lifetime telemetry leakage. |
| `node n8n/build-workflow.mjs process` and fixture builder | Both exited zero. |
| `node n8n/export-workflow.mjs process n8n/workflows/process.json` then `cmp` against the checked-in artifact | Byte-identical; graph/export tests also passed in the Node suite. |
| CI YAML structural assertions using Python `yaml.BaseLoader` | Passed for triggers, permissions, timeout, empty guarded variables, checkout paths, action setup and commands. First-party action pins checked against official repositories. `actionlint` was unavailable; hosted Actions has not run. |
| Changed-file scope and focused sensitive-pattern inspection | 31 changed/new files inspected; no dependency/migration drift or old prompt edits. Exactly two workflow Code bodies changed; inactive state, graph and credential/pin absence preserved. Selected private-key/token/connection-value patterns had no hits; this is not a full-history audit. |
| Local Markdown link/anchor checker; `git diff --check`; `git diff --cached --check` | Passed. The exact archived review body is excluded from link checking as historical user content. No changes staged. |

The first combined .NET run found an introduced validation-message regression: 68 passed, one failed, 19 skipped. Restoring the established message `Enter a single email mailbox.` fixed the rendered-page contract without weakening its assertion; the focused rebuilt Release test passed 1/1 before the final full rerun above.

The reviewed plan and all three implementation tasks received **APPROVE** after corrections. Task 2 review strengthened its lifetime-filter regression with a positive safe export marker. Final `sp_final_branch_reviewer` review returned **APPROVE**, with no Critical, Important or Minor findings. The reviewer inspected all 31 changed/new files, independently checked the final TRX and action pins, and reran the focused destination/Slack Node tests: 6/6 passed.

### Verification boundaries

The 19 PostgreSQL cases were deliberately skipped: this task does not authorize fixture writes to shared DEV. No Docker build, hosted Actions run, external collector check, n8n import/execution/activation or notification send was performed. Exception tests use TestServer and an in-process OTLP receiver; the standard middleware's request-aborted and already-started-response paths are not custom-handler guarantees. Local workflow corrections require separate authorized import and native validation before they describe the hosted runtime; historical live evidence still applies to its original revision.
